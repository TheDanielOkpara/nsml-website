# OS files
.DS_Store
Thumbs.db

# Source/unoptimised asset folders (not referenced from any page)
images/partners/PARTNERS LOGO/
images/sports-*.jpg

# CMS uploads — content, not code; keep the folder via .gitkeep only
cms/uploads/*
!cms/uploads/.gitkeep

# Real DB credentials — created on the server, never committed
cms/includes/config.local.php
